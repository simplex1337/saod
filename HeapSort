#include <stdio.h>
#include <time.h>
#include <stdlib.h>
#define n 3
#define m 100

int main()
{
	int a[m], no, i, j, c, min, temp;
	srand(time(NULL));
	for (i = 0; i < m; i++){
		a[i] = (rand());
	}
	for (i = 1; i < m; i++){
		c = i;
		do{
			min = (c - 1);
			if (a[min] < a[c]){
				temp = a[min];
				a[min] = a[c];
				a[c] = temp;
			}
			c = min;
		}
		while (c != 0);
	}
	for (j = m - 1; j >= 0; j--){
		temp = a[0];
		a[0] = a[j];
		a[j] = temp;
		min = 0;
		do{
			c = 2 * min + 1;
			if ((a[c] < a[c + 1]) && c < j-1)
				c++;
			if (a[min]<a[c] && c<j){
				temp = a[min];
				a[min] = a[c];
				a[c] = temp;
			}
			min = c;
		}
		while (c < j);
	}
	for (i = 0; i < m; i++)
		printf("\t %d\n", a[i]);
	return 0;
}
